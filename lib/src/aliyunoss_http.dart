
class AliyunOssHttp {
  static Duration connectTimeout = Duration(milliseconds: 5000);
  static Duration receiveTimeout = Duration(milliseconds: 60000) ;
  static Duration sendTimeout = Duration(milliseconds: 300000);
}
